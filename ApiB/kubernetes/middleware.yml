apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: api-b-selvin-ingress-path-strip
  namespace: capacitaciones
spec:
  stripPrefix:
    forceSlash: false
    prefixes:
    - /corporativo/it/api-b-selvin
